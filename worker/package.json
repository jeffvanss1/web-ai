{
  "name": "spotube-dj-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "The Cloudflare Worker behind Spotube DJ: Gemini calls, the spoken DJ, and D1 state.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node --test \"test/*.mjs\"",
    "check": "node --check src/index.js && wrangler deploy --dry-run --outdir .build"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
